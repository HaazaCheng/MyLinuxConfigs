set -xg GOROOT /opt/go
set -xg GOPATH ~/work/go
set -xg PATH /usr/local/sbin /usr/local/bin /usr/bin
set -xg PATH $GOPATH/bin $PATH
set -xg PATH ~/work/perl/perf-tools/bin $PATH
set -xg PATH ~/work/perl/FlameGraph $PATH
set -xg PATH ~/work/perl/HeatMap $PATH
set -xg BROWSER google-chrome-stable
